<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Graph Fraud Detection Paper Dashboard</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700&family=IBM+Plex+Mono:wght@400;600&display=swap" rel="stylesheet">
  <link href="https://unpkg.com/gridjs/dist/theme/mermaid.min.css" rel="stylesheet" />
  <script src="https://cdn.plot.ly/plotly-2.29.1.min.js"></script>
  <script src="https://unpkg.com/gridjs/dist/gridjs.umd.js"></script>
  <style>
    :root {
      --bg: #0f172a;
      --panel: #0b1224;
      --card: #111a2f;
      --accent: #2dd4bf;
      --accent-2: #38bdf8;
      --muted: #94a3b8;
      --text: #e2e8f0;
      --shadow: 0 16px 60px rgba(0, 0, 0, 0.35);
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: 'Manrope', system-ui, -apple-system, sans-serif;
      background: radial-gradient(120% 120% at 10% 10%, rgba(56,189,248,0.12), transparent 50%),
                  radial-gradient(90% 90% at 85% 15%, rgba(45,212,191,0.12), transparent 40%),
                  var(--bg);
      color: var(--text);
      line-height: 1.6;
    }
    header {
      padding: 48px 5vw 16px;
    }
    h1 {
      margin: 0 0 10px;
      font-size: clamp(28px, 4vw, 40px);
      letter-spacing: -0.5px;
    }
    .lede {
      max-width: 720px;
      color: var(--muted);
      margin-bottom: 28px;
    }
    .pill {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      border: 1px solid rgba(56,189,248,0.35);
      color: var(--accent-2);
      padding: 8px 14px;
      border-radius: 999px;
      font-weight: 600;
      background: rgba(56,189,248,0.08);
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 16px;
      margin-top: 20px;
    }
    .card {
      background: linear-gradient(145deg, var(--card), #0d1628);
      border: 1px solid rgba(255,255,255,0.05);
      border-radius: 14px;
      padding: 18px;
      box-shadow: var(--shadow);
    }
    .card h3 {
      margin: 0 0 4px;
      font-size: 16px;
      color: var(--muted);
    }
    .card .value {
      font-size: 26px;
      font-weight: 700;
      letter-spacing: -0.5px;
    }
    .section {
      padding: 28px 5vw;
    }
    .section h2 {
      margin: 0 0 14px;
      font-size: 20px;
      letter-spacing: -0.3px;
    }
    .section p {
      color: var(--muted);
      margin-top: 0;
    }
    .panel {
      padding: 16px;
      border-radius: 14px;
      background: var(--panel);
      border: 1px solid rgba(255,255,255,0.04);
      box-shadow: var(--shadow);
    }
    #table { margin-top: 8px; }
    a { color: var(--accent-2); text-decoration: none; }
    a:hover { text-decoration: underline; }
    ul.insights {
      padding-left: 18px;
      color: var(--text);
    }
    .pill-bar {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin: 12px 0 0;
    }
    .pill-bar .tag {
      border: 1px solid rgba(148,163,184,0.4);
      color: var(--text);
      padding: 6px 10px;
      border-radius: 999px;
      font-size: 13px;
    }
    .resources {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 10px;
    }
    .resource-card {
      padding: 12px 14px;
      background: rgba(255,255,255,0.03);
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.05);
    }
    footer {
      padding: 20px 5vw 36px;
      color: var(--muted);
      font-size: 13px;
    }
    @media (max-width: 720px) {
      header, .section { padding: 22px; }
    }
  </style>
</head>
<body>
  <header>
    <span class="pill">Graph & Transformer Fraud Detection Papers</span>
    <h1>Interactive summary of the Safe-Graph curated list</h1>
    <p class="lede">
      Visualize trends, venues, open-source availability, and hotspots across graph/transformer-based fraud and anomaly detection research.
      Data is sourced directly from the upstream GitHub repository and can refresh automatically on new commits.
    </p>
    <div class="grid">
      <div class="card">
        <h3>Total papers</h3>
        <div class="value" id="stat-paper-count">–</div>
      </div>
      <div class="card">
        <h3>Code availability</h3>
        <div class="value" id="stat-code">–</div>
      </div>
      <div class="card">
        <h3>Top venue</h3>
        <div class="value" id="stat-venue">–</div>
      </div>
      <div class="card">
        <h3>Dominant topic</h3>
        <div class="value" id="stat-topic">–</div>
      </div>
    </div>
  </header>

  <section class="section">
    <h2>What stands out</h2>
    <div class="card">
      <ul class="insights" id="insight-list"></ul>
    </div>
  </section>

  <section class="section">
    <h2>Distribution snapshots</h2>
    <p>Use the hover tooltips to read exact counts.</p>
    <div class="grid">
      <div class="panel"><div id="year-chart" style="height:320px;"></div></div>
      <div class="panel"><div id="category-chart" style="height:320px;"></div></div>
      <div class="panel"><div id="venue-chart" style="height:320px;"></div></div>
      <div class="panel"><div id="topic-chart" style="height:320px;"></div></div>
      <div class="panel"><div id="language-chart" style="height:320px;"></div></div>
    </div>
  </section>

  <section class="section">
    <h2>Interactive paper table</h2>
    <p>Search titles, filter by venue or category, and click links to open the paper or code.</p>
    <div class="panel">
      <div id="table"></div>
    </div>
  </section>

  <section class="section">
    <h2>Resources, toolboxes, surveys</h2>
    <p>Curated extras from the upstream README.</p>
    <div id="resource-container" class="resources"></div>
  </section>

  <footer>
    Built with Plotly and Grid.js. Regenerate via GitHub Actions on push/dispatch/schedule.
  </footer>

  <script>
    const papers = {{ papers_json | safe }};
    const stats = {{ stats_json | safe }};
    const resources = {{ resources_json | safe }};

    const fmtNumber = (n) => n?.toLocaleString("en-US");

    function populateStats() {
      document.getElementById("stat-paper-count").textContent = fmtNumber(stats.paper_count || papers.length);
      document.getElementById("stat-code").textContent = stats.code_availability
        ? `${fmtNumber(stats.code_availability.with_code || 0)} (${stats.code_availability.percentage || 0}%)`
        : "–";
      document.getElementById("stat-venue").textContent = stats.venue_counts?.length ? stats.venue_counts[0].venue : "–";
      document.getElementById("stat-topic").textContent = stats.topics?.length ? stats.topics[0].topic : "–";

      const insightList = document.getElementById("insight-list");
      if (stats.insights?.length) {
        stats.insights.forEach((line) => {
          const li = document.createElement("li");
          li.textContent = line;
          insightList.appendChild(li);
        });
      } else {
        insightList.innerHTML = "<li>No insights available.</li>";
      }
    }

    function chart(container, data, layoutExtras = {}) {
      const baseLayout = {
        margin: {l: 50, r: 20, t: 30, b: 50},
        paper_bgcolor: 'rgba(0,0,0,0)',
        plot_bgcolor: 'rgba(0,0,0,0)',
        font: {color: '#e2e8f0', family: 'Manrope'},
      };
      Plotly.newPlot(container, data, {...baseLayout, ...layoutExtras}, {displayModeBar: false, responsive: true});
    }

    function buildCharts() {
      const years = stats.year_counts || [];
      chart("year-chart", [{
        x: years.map(d => d.year),
        y: years.map(d => d.count),
        type: "bar",
        marker: {color: "#38bdf8"}
      }], {title: "Papers by Year"});

      chart("category-chart", [{
        x: stats.category_counts?.map(d => d.count),
        y: stats.category_counts?.map(d => d.category),
        type: "bar",
        orientation: "h",
        marker: {color: "#2dd4bf"}
      }], {title: "Entries by Category", height: 320, margin: {l: 120, r: 20, t: 30, b: 40}});

      chart("venue-chart", [{
        x: stats.venue_counts?.map(d => d.count),
        y: stats.venue_counts?.map(d => d.venue),
        type: "bar",
        orientation: "h",
        marker: {color: "#818cf8"}
      }], {title: "Top Venues", height: 360, margin: {l: 160, r: 20, t: 30, b: 40}});

      chart("topic-chart", [{
        x: stats.topics?.map(d => d.topic),
        y: stats.topics?.map(d => d.count),
        type: "bar",
        marker: {color: "#f472b6"}
      }], {title: "Frequent Title Terms", height: 320});

      if (stats.language_counts?.length) {
        chart("language-chart", [{
          labels: stats.language_counts.map(d => d.language),
          values: stats.language_counts.map(d => d.bytes),
          type: "pie",
          hole: 0.4
        }], {title: "Language share (code repos)", height: 320});
      } else {
        document.getElementById("language-chart").innerHTML = "<p style='color:#94a3b8'>No code language data yet.</p>";
      }
    }

    function buildTable() {
      const rows = papers.map((p) => [
        p.year || "—",
        gridjs.html(`<a href="${p.paper_url || '#'}" target="_blank" rel="noopener">${p.title}</a>`),
        p.venue || "—",
        p.category || "—",
        p.subcategory || "—",
        gridjs.html(p.code_url ? `<a href="${p.code_url}" target="_blank" rel="noopener">Code</a>` : "—")
      ]);
      new gridjs.Grid({
        columns: ["Year", "Title", "Venue", "Category", "Section", "Code"],
        data: rows,
        search: true,
        sort: true,
        pagination: {enabled: true, limit: 15},
        style: {
          table: {'background-color': 'transparent'},
          th: {'background-color': '#0f172a', 'color': '#e2e8f0'},
          td: {'color': '#e2e8f0'}
        }
      }).render(document.getElementById("table"));
    }

    function buildResources() {
      if (!resources?.length) {
        document.getElementById("resource-container").innerHTML = "<p style='color:#94a3b8'>No extra resources parsed.</p>";
        return;
      }
      const container = document.getElementById("resource-container");
      resources.forEach((res) => {
        const div = document.createElement("div");
        div.className = "resource-card";
        div.innerHTML = `<div style="color:#94a3b8;font-size:12px;text-transform:uppercase;letter-spacing:1px;">${res.category}</div>
                         <a href="${res.url}" target="_blank" rel="noopener">${res.title}</a>`;
        container.appendChild(div);
      });
    }

    populateStats();
    buildCharts();
    buildTable();
    buildResources();
  </script>
</body>
</html>
